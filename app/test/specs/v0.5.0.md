# v0.5.0 テスト仕様書

[roadmap v0.5.0](../../docs/roadmap.md) の実装範囲に対応する。製品仕様は [docs/specs/](../../docs/specs/) が正。

## スコープ

- **`build()`** — エントリ列挙 + glob → レンダリング → `outDir` へ書き出し（[api.md](../../docs/specs/api.md)）
- **glob** — picomatch + fast-glob（Vite/JS エコシステム互換）
- **`_` partial 除外** — マッチ後フィルタ（glob 明示 / `includeUnderscoreEntries` で上書き可 → [paths.md](../../docs/specs/paths.md)）
- **依存グラフ走査** — include / component / extend を静的収集（**出力はエントリのみ**。走査は検証・グラフ構築用）
- **`serverContext`** — `renderServer` / `build` のみ（別引数。template context にマージ）
- **リモート loader** — Node デフォルト loader が `http://` / `https://` を fetch（include / component / extend）
- **最小 `renderClient`** — export のみ。`serverContext` 渡し → **`server_context_on_client`**
- v0.4 回帰（319 テスト）維持

## スコープ外（v0.5.0）

- **`renderClient` 本実装**（loader 必須 CSR）— [roadmap v0.6](../../docs/roadmap.md)
- **依存先を追加エントリとして build 出力** — entries-only
- **`suspicious_context_value`** — v0.7
- context 秘密キー名の機械検出 — [api.md](../../docs/specs/api.md) 非ゴール

## 確定事項（v0.5.0）

| 項目 | 確定内容 |
|------|----------|
| 公開 API 追加 | `build(options)`, `renderClient`（stub）, `BuildOptions` / `BuildResult` 型 |
| デフォルト loader | Node: **`createNodeLoader()`** = FS + リモート fetch。`renderServer` / `build` の loader 省略時に使用 |
| outDir 映射 | **root からの相対パスを outDir にミラー**（`pages/a.md` → `outDir/pages/a.md`） |
| build 出力対象 | **glob/列挙で得たエントリのみ**。依存走査は検証・グラフ用（依存先は単体ファイルとして出力しない） |
| `_` 除外 | ファイル名 `_` 始まり、または `_` で始まるディレクトリ配下 → **エントリから除外** |
| `_` 上書き | input glob が `_partials/**` 等で **明示マッチ**したパス、または `includeUnderscoreEntries: true` → エントリに含める |
| glob 基準 | `options.root` または `.doc_root` 探索結果を **cwd** として fast-glob 実行 |
| 依存収集 | **静的**（render 前）。`scanHgBlocks` + 既存 parser（include / component / extend） |
| 依存グラフ | エントリから BFS/DFS。ノード = 絶対パス or URL。辿った依存は graph に含むが **出力しない** |
| 欠落依存 | build 前検証で `file_not_found` → **中断**（render 時と同型） |
| serverContext マージ | `mergeContext(context, serverContext)` — **serverContext 後勝ち**（[renderServer.ts](../../src/renderServer.ts) 既存順序） |
| serverContext 利用 | `renderServer` / `build` のみ。template の `{{ key }}` から参照可 |
| リモートパス | `isRemotePath(url)` = `http://` / `https://` 始まり。`resolveTemplatePath` **bypass** し loader に URL 直渡し |
| リモート + doc_root | URL は root 制約**外**。FS パスのみ `constrainToRoot` |
| renderClient stub | `serverContext` 非 null/undefined → **`server_context_on_client`**。それ以外は v0.6 まで未実装（専用 code または `parse_error` — テストで固定） |
| build 戻り値 | `{ files: { path, markdown }[], warnings }`。`path` は **outDir からの相対**または root 相対 — **root 相対で固定** |
| エントリ重複 | 同一相対パスが複数 input でマッチ → **後勝ち**（warning なし） |
| 書き出し | `fs/promises.mkdir` recursive + `writeFile`。親ディレクトリ自動作成 |
| extend + remote | v0.5 loader 拡張で **サポート**（v0.4 で deferred 分を含む） |

## fixture 構成（想定）

```
app/test/fixtures/v0.5/
  glob/
    doc-root/
      .doc_root
      pages/
        index.md
        about.md
      _partials/
        header.md
  underscore/
    pages/
      visible.md
    _partials/
      hidden.md
    _hidden-dir/
      secret.md
  deps/
    entry.md
    partials/
      included.md
    components/
      widget.md
    layouts/
      base.md
  remote/
    parent-local.md
    remote-body.md          # mock fetch 用（実体はテスト内 stub）
  build-basic/
    doc-root/
      .doc_root
      pages/
        hello.md
      partials/
        greeting.md
  server-context/
    page.md
  render-client/
    page.md                 # stub テスト用（render 本体は v0.6）
```

---

## Phase 1: serverContext

対象: [renderServer.ts](../../src/renderServer.ts) / 将来の `build()`

`renderServer` は既に `mergeContext(context, options?.serverContext)` を実行。

### renderServer（serverContext）

#### テスト: 正常系

- `renderServer(source, { title: "public" }, { serverContext: { apiKey: "secret" } })` → template で `{{ apiKey }}` が `"secret"`
- `context` と `serverContext` に同名キー → **serverContext の値**が使われる
- `serverContext` 未指定 → 従来どおり（v0.4 回帰）

#### テスト: 異常系

- （serverContext 自体に parse エラーは無し。値は任意 unknown）

### build（serverContext）

#### テスト: 正常系

- `build({ input, outDir, serverContext: { env: "prod" } })` → エントリ template で `{{ env }}` が展開
- warnings / files が返る

---

## Phase 2: エントリ収集 + `_` フィルタ

新規 `app/src/build/`:

| 関数 | 役割 |
|------|------|
| `isUnderscorePartial(path)` | `_` ファイル / `_` ディレクトリ配下判定 |
| `collectEntryPaths(input, options)` | glob 展開 + `_` フィルタ |

依存: `fast-glob`, `picomatch`（runtime dependencies）

### isUnderscorePartial

#### テスト: 正常系

- `_meta.md` → true
- `_partials/card.md` → true（`_partials` 配下）
- `pages/index.md` → false
- `partials/_card.md` → true（[paths.md](../../docs/specs/paths.md): ファイル名が `_` で始まる）

### collectEntryPaths

#### テスト: 正常系

- 単一ファイルパス → 1 件（絶対パス正規化）
- `pages/**/*.md` glob → マッチした `.md` 一覧
- `_partials/*.md` は **デフォルト除外**
- input が `_partials/hidden.md` を **明示** → 1 件含む
- `includeUnderscoreEntries: true` → `_` ファイルも glob マッチ分含む
- `options.root` 指定時、そのディレクトリ基準で glob

#### テスト: 異常系

- 空 input / マッチ 0 件 → 空配列（エラーにしない）
- 存在しない単一パス → **`file_not_found`** または空 — **file_not_found で固定**

---

## Phase 3: 依存グラフ

| 関数 | 役割 |
|------|------|
| `collectDependencies(source, fromPath)` | 1 ソースから include / component / extend パス列 |
| `buildDependencyGraph(entries, loader, options)` | エントリから辿る。欠落検証 |

### collectDependencies

#### テスト: 正常系

- `include ./partials/body.md` → `{ kind: "include", path: "./partials/body.md" }`
- `component ./c.md as c` → `{ kind: "component", path: "./c.md" }`
- extend ブロック先頭 `extend ./layouts/base.md` → `{ kind: "extend", path: "./layouts/base.md" }`
- 同一ファイルに複数依存 → 出現順に列挙
- if/each/const のみ → 空列

#### テスト: 異常系

- 壊れた hyogen → **`parse_error`**（execute 段と同型）

### buildDependencyGraph

#### テスト: 正常系

- エントリ 1 件 + include 先 → graph に 2 ノード、エッジ 1 本
- extend 先 layout 内の include も **再帰的に** graph に追加
- component パスは **登録先ファイル**をノードに（`as` 名ではない）
- 戻り値に `entries` / `dependencies` / `edges` 等 — 実装方針をテストで固定

#### テスト: 異常系

- 依存先不存在 → **`file_not_found`**（build 中断前に検出）

---

## Phase 4: リモート loader

新規 `app/src/io/createNodeLoader.ts`, `isRemotePath.ts`

### isRemotePath

#### テスト: 正常系

- `https://example.com/a.md` → true
- `http://127.0.0.1/x` → true
- `./local.md`, `/abs.md` → false

### createNodeLoader

#### テスト: 正常系

- ローカル既存ファイル → 内容返却（createFsLoader 互換）
- `https://...` → `fetch` 呼び出し、response.text() を返却
- `renderServer` + `include https://...` → mock fetch で本文展開

#### テスト: 異常系

- fetch 404 → **`file_not_found`**
- fetch ネットワークエラー → **`load_failed`**
- ローカル不存在 → **`file_not_found`**

### パス解決 bypass

対象: [expandIncludes.ts](../../src/include/expandIncludes.ts), [expandExtendsAndBlocks.ts](../../src/layout/expandExtendsAndBlocks.ts), [renderComponent.ts](../../src/component/renderComponent.ts)

#### テスト: 正常系

- remote include URL が `resolveTemplatePath` を通らず loader に渡る
- remote component / extend も同様

---

## Phase 5: build()

新規 `app/src/build/build.ts`

```ts
type BuildOptions = RenderOptions & {
  input: string | string[];
  outDir: string;
  includeUnderscoreEntries?: boolean;
  context?: HyogenContext;
  serverContext?: HyogenContext;
};

type BuildResult = {
  files: { path: string; markdown: string }[];
  warnings: HyogenWarning[];
};
```

### build

#### テスト: 正常系

- `input: ["pages/hello.md"]` → 1 ファイルレンダリング + `outDir/pages/hello.md` 作成
- **ミラー映射**: `pages/nested/a.md` → `outDir/pages/nested/a.md`
- `files[].path` は root からの相対パス
- `files[].markdown` はレンダリング結果（front matter strip デフォルト）
- 複数エントリ glob → 各ファイル出力
- include 先 partial は **outDir に単体出力されない**（entry の markdown に埋め込みのみ）
- warnings を全エントリ分集約
- `context` + `serverContext` 両方適用

#### テスト: 異常系

- エントリ 1 件が `parse_error` → **build 全体中断**（throw）
- `outDir` 書き込み不可 → **`load_failed`** または Node errno — **load_failed で固定**
- 依存 graph 上の欠落 → **`file_not_found`**

---

## Phase 6: renderClient stub + 公開面

新規 `app/src/renderClient.ts`

### renderClient

#### テスト: 正常系

- （v0.5 では正常系 render 無し — v0.6）

#### テスト: 異常系

- `renderClient("# Hi", {}, { serverContext: { x: 1 } })` → **`server_context_on_client`**
- `serverContext: undefined` / 未指定 → stub の未実装エラー（v0.6 まで）。**serverContext チェックが先**

### index export

#### テスト: 正常系

- `import { build, renderClient, BuildOptions, BuildResult } from "hyogen-md"` が型解決（dist ビルド後）

---

## Phase 7: 統合シナリオ（end-to-end）

[roadmap v0.5.0](../../docs/roadmap.md) の「テスト」節 4 項目。`app/test/fixtures/v0.5/` + `build()` / `renderServer` で検証。

#### テスト: 正常系

- **glob + `_` フィルタ**: `glob/` — `pages/*.md` のみ build。`_partials` は除外。明示 glob で包含
- **依存グラフ走査**: `deps/entry.md` — graph に include / component / extend 先が現れる
- **serverContext**: `server-context/page.md` — `{{ secretKey }}` が serverContext から展開（build + renderServer）
- **リモート include**: mock fetch — `remote/parent-local.md` が URL 本文を include

#### テスト: 異常系

- **renderClient + serverContext**: `render-client/` → **`server_context_on_client`**
- v0.4 回帰 fixture Green

---

## テストファイル対応（想定）

| モジュール | テストファイル |
|-----------|----------------|
| `isUnderscorePartial` / `collectEntryPaths` | `test/build/collectEntryPaths.test.ts`（新規） |
| `collectDependencies` / `buildDependencyGraph` | `test/build/dependencyGraph.test.ts`（新規） |
| `isRemotePath` / `createNodeLoader` | `test/io/createNodeLoader.test.ts`（新規） |
| `build` | `test/build/build.test.ts`（新規） |
| `renderServer` serverContext | `test/renderServer.v0.5.test.ts`（新規） |
| `renderClient` stub | `test/renderClient.test.ts`（新規） |
| E2E | `test/build.v0.5.integration.test.ts`（新規） |

---

以上
