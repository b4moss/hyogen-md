# v0.12.0 ロードマップ（アーカイブ）

完了済み。現行計画は [roadmap.md](../../roadmap.md)。

---

## v0.12.0 — TOC / `.length` / `echo` / データソース配線

ライブラリの表現力を拡張する機能リリース。製品仕様の正は [specs/](../../specs/)。

### 実装

- [x] **TOC 専用ヘルパ** — `toc` / `toc(N)`（[toc.md](../../specs/toc.md)、Issue #42）
- [x] **許可プロパティ `.length`** — 配列・文字列（メソッド `length()` は不可。Issue #45）
- [x] **`echo <式>`** — `@hg` / `@@` ブロックを評価結果で本文置換（Issue #53）
- [x] **データソース API 配線** — `dataSources` / `loadDataSources` を `renderServer` / `build` に統合（YAML / JSON / CSV。Issue #36 / #37）
- [x] docs-site: TOC・許可プロパティ・`echo`・dataSources の利用者向けページ（Issue #44 / #47 等）
- [x] CI/CD: app / docs 分離、Pages を **`doc-site`** ブランチからデプロイ
- [x] `app/package.json` version を **`0.12.0`** に更新（= tag `v0.12.0`）

### スコープ外（後続）

- 許可メソッド追加（`.slice` 等）→ [v0.13.0](https://github.com/b4moss/hyogen-md/milestone/4)

### テスト

- [x] テスト仕様: [app/test/specs/v0.12.0.md](../../../app/test/specs/v0.12.0.md)（TOC）および `.length` / `echo` の自動テスト

### リリース

- git tag **`v0.12.0`** / npm **`@b4moss/hyogen-md@0.12.0`**

----

以上
