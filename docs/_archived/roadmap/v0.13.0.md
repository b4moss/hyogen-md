# v0.13.0 ロードマップ（アーカイブ）

完了済み。現行計画は [roadmap.md](../../roadmap.md)。

---

## v0.13.0 — CLI（create / config / dev / build）

執筆体験を Vite 寄りにする CLI リリース。製品仕様の正は [specs/cli.md](../../specs/cli.md)。

### 実装

- [x] **`hyogen-md create`** — 最小プロジェクト生成（`package.json` / `hyogen.config.(js|ts)` / `src/index.md` / `.gitignore`）。Issue #35
- [x] **`hyogen.config.(js|ts)` + `defineConfig`** — `@b4moss/hyogen-md/config`。必須は `input`、`outDir` 既定 `./out`。Issue #35
- [x] **`hyogen-md build`** — 設定を既存 `build()` に渡して `outDir` へ出力
- [x] **`hyogen-md dev`** — ファイルツリー + プレビュー、ファイルウォッチ、ファイル単位 HMR。`outDir` には書かない。Issue #30
- [x] テスト仕様: [app/test/specs/v0.13.0.md](../../../app/test/specs/v0.13.0.md)
- [x] docs-site: CLI 利用者向けページ（EN/JA）
- [x] `app/package.json` version を **`0.13.0`** に更新（= tag `v0.13.0`）

### スコープ外（後続）

- 許可メソッド追加（`.slice` 等）→ [Until v1.0.0](https://github.com/b4moss/hyogen-md/milestone/2) / Issues
- component 挿入時の見出し階層適合 → [#50](https://github.com/b4moss/hyogen-md/issues/50)

### リリース

- git tag **`v0.13.0`** / npm **`@b4moss/hyogen-md@0.13.0`**

----

以上
